基本設計

- システム構成

```
ユーザがDiscordでコマンド入力する
↓
DiscordAPIがサーバにリクエスト送信
↓
サーバ: リクエストを受信
↓
Bot: index.ts
↓
DB: 問題取得
↓
Bot: index.ts
↓
サーバ: DiscordAPIに問題を送信
↓
DiscordAPI: 個人サーバに問題を送信
↓
ユーザに問題を表示する
```

- 使用言語等
```
開発言語： TypeScript(今後Python)
ライブラリ： discord.js
実行環境： Node.js
データベース： PostgreSQL
開発環境： Docker
本番環境： Oracle Cloud Always Free + Coolify
```

- 使用コマンド
/ regiter : 問題を登録する。（今後、AIで登録するように実装するため、今後不要になる）
/ question : 問題を取得し、送信する。

- ディレクトリ構成
```
discord-bot/src/
   └ commands
   └ deploy-commands.ts
   └ events
   └ index.ts
```

- エラー設計
問題が存在しない
↓
「出題する問題がありません。」

サーバが起動していない
↓
「アプリケーションが応答しませんでした」

- データ設計
```
interface Coordinate {
    latitude: number;
    longitude: number;
}

interface Location {
    prefecture: string;
    city: string;
    coordinate: Coordinate;
}

interface NewQuestion {
    location: Location;
    imageUrl: string;
}

interface Question extends NewQuestion {
    id: number;
}
```